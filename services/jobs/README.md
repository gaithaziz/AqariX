# AqariX Background Jobs

This directory contains the Python background tasks, scheduled worker jobs, and recommendation/forecasting model scripts.

## Setup

1. Copy `.env.example` to `.env` and fill in necessary fields.
2. Initialize environment and dependencies.
3. Run target worker tasks.
