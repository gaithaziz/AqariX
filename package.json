{
  "name": "aqarix",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "web:dev": "pnpm --dir apps/web dev",
    "web:build": "pnpm --dir apps/web build",
    "web:lint": "pnpm --dir apps/web lint",
    "api:test": "cd services/api && uv run --extra dev pytest",
    "api:lint": "cd services/api && uv run --extra dev ruff check ."
  }
}
